{% extends 'base.html.twig' %}

{% block title %}Hello UsersController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">

    <h1>Hello</h1>

    {{ dump(users) }}

    <ul>
    {% for user in users %}
        <li>{{ user.name }} - {{ user.datecrea|date("d/m/Y à H:i:s") }}</li>
    {% endfor %}
    </ul>

    <a href="{{ path('users_add') }}" class="btn btn-primary btn-lg active" role="button" aria-pressed="true">Creer un User</a>
    <a href="{{ path('articles_new') }}" class="btn btn-success btn-lg active" role="button" aria-pressed="true">Creer un Article</a>

</div>
{% endblock %}
